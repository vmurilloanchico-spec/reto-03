import { CircularLinkedList } from './circular-linked-list';

describe('CircularLinkedList', () => {
  it('should create an instance', () => {
    expect(new CircularLinkedList()).toBeTruthy();
  });
});
